import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 10,
    height: 120,
    backgroundColor: "gray",
    borderRadius: 20,
    padding: 20,
  },
});
